import React from 'react';
import './index.css';

const UnderlineTitle = ({title})=> {
  return (
    <div className="underlineTitle fullWidth">
      <span className="mainTitle">{title}</span>
    </div>
  )
};


export default UnderlineTitle;
